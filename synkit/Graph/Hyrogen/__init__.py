"""Hydrogen graph completion.

The provenance-aware implementation is available from :mod:`.hcomplete` and
:mod:`.hextend`. Historical same-permutation behavior is isolated in
:mod:`.hcomplete_legacy` and :mod:`.hextend_legacy`.
"""
